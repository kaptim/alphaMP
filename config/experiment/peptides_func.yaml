# @package _global_
defaults:
  - model: mpnn-peptides_func

training:
  lr: 0.002
  epochs: 200
  warmup: 10
  weight_decay: 0.0
  batch_size: 32

model:
  node_embed: true
  edge_embed: true
  global_pool: mean
