# @package _global_
defaults:
  - model: ogbg_molhiv

training:
  lr: 0.002
  epochs: 100
  warmup: 5
  weight_decay: 0.0
  batch_size: 512

model:
  node_embed: true
  edge_embed: true
  dropout: 0.4
  global_pool: mean
