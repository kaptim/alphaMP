# @package _global_
defaults:
  - model: mpnn_a_095

training:
  lr: ${model.lr}
  epochs: 200
  warmup: 5
  weight_decay: 0.0
  batch_size: 512

model:
  node_embed: true
  edge_embed: true
  dropout: 0.4
  global_pool: mean
