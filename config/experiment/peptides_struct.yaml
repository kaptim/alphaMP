# @package _global_
defaults:
  - model: mpnn-peptides_struct

training:
  lr: 0.004
  epochs: 200
  warmup: 10
  weight_decay: 0.0
  batch_size: 32

model:
  node_embed: true
  edge_embed: true
  global_pool: mean
