# @package _global_
defaults:
  - model: mpnn-ogbg_code2

training:
  lr: 0.0003
  epochs: 30
  warmup: 2
  weight_decay: 0.0
  batch_size: 32

model:
  node_embed: true
  edge_embed: false
  global_pool: mean
  head: code2
