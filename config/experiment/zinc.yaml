# @package _global_
defaults:
  - model: mpnn

training:
  lr: ${model.lr}
  epochs: 500
  warmup: 50
  weight_decay: 0.0
  batch_size: 50

model:
  node_embed: true
  edge_embed: true
  global_pool: sum